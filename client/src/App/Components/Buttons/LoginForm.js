import React from "react";
import { Button } from "react-bootstrap";

export default function LoginFormButton() {
  return (
    <Button block size="md" variant="primary" type="submit">
      Log in
    </Button>
  );
}
